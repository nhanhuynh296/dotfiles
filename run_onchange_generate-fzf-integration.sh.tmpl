#!/usr/bin/env bash
set -euo pipefail

# Re-runs only when fzf's version changes: {{ output "fzf" "--version" | trim }}

mkdir -p "$HOME/.cache/fzf"
fzf --zsh > "$HOME/.cache/fzf/integration.zsh"

echo "Regenerated fzf zsh integration cache"
